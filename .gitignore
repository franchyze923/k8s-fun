# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/terraform.tfvars

# Generated files with credentials/secrets
kubeconfig
dashboard-token.txt
s3-credentials.txt
config.yml

# Ansible inventory (contains IPs, generated by terraform)
ansible/inventory.ini

# Deployment state
.k8s-distro
.lb-implementation
.cluster-name
.vm-id-start

# Old unused files
config.env

# OS files
.DS_Store
argocd-credentials.txt
keycloak-credentials.txt

# Deployment artifact backups
deployments/

# TLS certs (contains private keys)
tls-certs/
